pipeline {
    agent any 
	stages {
		stage('Git Checkout') {
			steps {
				echo "Git checkout `date`"
					}
			}
		stage('Build Code') {
			steps {
				echo " Code Rebuild `date`"
			}
		}
		
		stage('Deploy Code") {
			steps {
				echo "Code Deployed `date`"
			}
		}
               }
}

			

